{
  "name": "http-server",
  "version": "1.0.0",
  "description": "A simple HTTP server for serving HTML files.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js --port 3000"  // You can set a default port
  },
  "dependencies": {
    "minimist": "^1.2.5"  // Check for the latest version
  },
  "author": "",
  "license": "ISC"
}
